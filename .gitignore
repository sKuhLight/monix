# build output
/build/
/app/build/
/lib/build/

# kernel module artifacts
*.ko
*.o
*.mod
*.mod.c
*.mod.o
.*.cmd
*.symvers
modules.order
Module.symvers
.tmp_versions/

# reverse-engineering: keep only the documented helper scripts; never commit the
# proprietary binary, ad-hoc probe tools, or their compiled output
/re/*
!/re/dis.sh
!/re/dumpconst.py
!/re/README.md

# capture artifacts
capture/*.pcapng
capture/*.pcap

# runtime UI state
imgui.ini
